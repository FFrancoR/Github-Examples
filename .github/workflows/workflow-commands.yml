name: workflow-commands

on: ['push']

jobs:
    my_job:
        runs-on: ubuntu-latest
        steps:
            - name: "Group Logging"
              runs: |
                echo "::group::My Group Message
                echo "Msg 1"
                echo "Msg 2"
                echo "::endgroup::"