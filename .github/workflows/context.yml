name: "Context"

on: ["push"]

jobs:
    my-context:
        runs-on: ubuntu-latest
        steps:
            - name: "My step"
              run: echo "Hello world! $MY_ACTION $MY_ACTOR"
              env:
                  MY_ACTION: ${{github.action}}
                  MY_ACTOR: ${{github.actor}}
